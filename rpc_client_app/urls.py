from django.urls import path 
from . import views

app_name = "rpc_client_app"

rpc_client_app_urls = [
    # path('populate/bookable/service/', views.populate_bookable_service , name="populate_bookable_service" ),
]